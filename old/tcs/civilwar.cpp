#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
    int N;
    cin >> N;

    vector<long long> powers(N);
    for (int i = 0; i < N; i++)
    {
        cin >> powers[i];
    }
  
  

return 0;
}
